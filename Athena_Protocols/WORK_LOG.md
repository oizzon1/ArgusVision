# Work Log — ArgusVision PhD

**Purpose:** session-level operational memory: hours worked, work completed, key issues, findings, and notes that are too granular for `ATHENA_STATE.md`.

**Update rule:** update this file at every session close, even if no code or roadmap file changed. `ATHENA_STATE.md` remains the milestone ledger; this file is the day-to-day work record.

---

## Logging Protocol

At session close, append one entry under the current month using this shape:

```markdown
### YYYY-MM-DD — Mode(s): STRATEGIST / ADVISOR / OPERATOR / REVIEWER

| Field | Entry |
|---|---|
| Work hours | 0.0 h |
| Work done | ... |
| Key issues | ... |
| Findings | ... |
| Notes | ... |
| Files changed | ... |
| Verification | ... |
| Next actions | ... |
```

Rules:
- **One entry per day** (merge multiple sessions of the same day into one entry).
- **Rolling update while a session is open** (2026-08-04): refresh the day's
  entry every **~30 minutes of active work**, and every **~20 minutes of idle**
  when a long job is running or the user is away. The entry carries a
  `TODO SEQUENCE (restore point)` field listing the remaining steps in order,
  so a session that opens cold — or one that closes unexpectedly — can resume
  mid-stream without re-deriving the plan. Mark the entry **SESSION OPEN** with
  a timestamp while live; stamp the close when it ends.
- Idle refreshes record what is *running*, what it is *waiting on*, and what
  must not be touched meanwhile — an idle log entry that says only "waiting" is
  not a restore point.
- **A day's entry may not be closed while a background job is still running**
  (user directive, 2026-08-04). While anything is executing — a build, an
  evaluation, a monitor — the entry stays marked **SESSION OPEN** and keeps
  being refreshed. Stamp the close only after the last job has completed and
  its outcome is recorded. A closed entry asserts that the session's work
  finished; closing it over a running job makes that assertion false, and the
  next session would inherit a job it does not know about.
- Record actual hours where known; otherwise write `not tracked`, never guess.
- Keep experimental numbers out unless they already exist in `results/` or the thesis.
- Use repo-relative file paths.
- Promote only durable milestones, decisions, and program-state changes to `ATHENA_STATE.md`.
- If the session ends because work is blocked, record the blocker and the exact next unblocking action.

---

## 2026-07

### 2026-07-28 — Mode(s): STRATEGIST / REVIEWER / OPERATOR

| Field | Entry |
|---|---|
| Work hours | **6.75 h** (20:00 → 02:45) |
| Work done | ATHENA v2.0 protocol suite (single identity, four modes; `ATHENA_STATE.md` ledger created and seeded from full thesis read; multi-agent bootstrap stubs `CLAUDE.md`/`GEMINI.md`/`AGENTS.md`); Publication Roadmap v2.0 adopted as canonical (VBB-vs-OBB letter cancelled into P2 ablation; Scientific Data → ISPRS OJ for P1; P3 conference paper added; RT-DETR-seg dropped); `papers/` WTF-P workspaces + `zenodo_release/` skeletons; line-ending policy (`.gitattributes`, killed 69-file/15,962-line CRLF phantom diff); environment audit: `thesis_env` → `AV_env` rename (clone→verify→remove), env spec corrected (pip cu124 torch, missing scipy) + 200-package lock file, doc rot fixed, `ATHENA_RISE.md` → v2.1; deep REVIEWER pass over `src/` (~10.5k lines) → `TODO_RESTRUCTURE.md` (findings F1–F6, target structure, 5-phase migration plan; committed next day 13:46). |
| Key issues | Env spec could not have rebuilt the env that produced thesis results; evaluation implemented 4× with disagreeing matchers (F1); `map` metric is macro-F1 (F2, ISPRS reviewer risk); adapters entangled with evaluation (F3). |
| Findings | Greedy matcher iterates predictions unsorted by confidence — low-conf pred can steal a GT from a high-conf pred (F2 bonus bug). Config corrected to match the working env, never the reverse. |
| Notes | STATE OPEN 4 closed (assets all local); OPEN 5 opened (Mask R-CNN on Windows: Detectron2 unsupported → MMDetection primary path, before Sep 2026). |
| Files changed | `Athena_Protocols/*`; `ArgusVision_Strategic_Planning/PUBLICATION_ROADMAP.md`; `CLAUDE.md`; `GEMINI.md`; `AGENTS.md`; `papers/**`; `zenodo_release/**`; `.gitattributes`; `ArgusVision_environment.{yml,lock.txt}`; `TODO_RESTRUCTURE.md` |
| Verification | torch 2.5.1+cu124 CUDA verified on RTX 3090 Ti; all 11 codebase imports resolve; bootstrap stubs identical; commits f735f0f, 3cd20d8, b6ca74a, 07f1a34 on `dev`. |
| Next actions | Roadmap revision after supervisor meeting; execute restructure; P0 Zenodo prep. |

### 2026-07-29 — Mode(s): STRATEGIST / OPERATOR / ADVISOR

| Field | Entry |
|---|---|
| Work hours | **7.17 h** (16:00 → 23:10 EEST) |
| Work done | **Roadmap → v2.1** (trigger: supervisor meeting finalized paper plan): P2 trimmed to pipeline + Mask R-CNN + YOLOv11-seg baselines (VBB-on-DOTA ablation → parked VBB-vs-OBB paper; Mask2Former out; experiments Dec→Oct 2026); P3 kept as insurance; P4 reframed detector × segmenter benchmark; parked-papers section added; STATE OPEN 1 closed. **Restructure phases 0–4 executed** on new branch `AV_dev` (user-named): tag `thesis-code-final`; installable `argusvision` package (`pyproject.toml`, editable into `AV_env`); domain constants consolidated; frozen evaluation stack (Hungarian + confidence-ranked matchers in one module, honest metric names `macro_f1`/`average_precision`, IoU 0.1/0.3/0.5, run manifests); predict-only YOLO/SAM adapters + registry + pipeline + runtime + viz (V1 evaluator + 3 dependent scripts deleted); `AerialFuseCVSplit`/`DotaYoloObbSplit` data classes; generic YAML driver `experiments/run_evaluation.py`; 62 tests passing; three validation runs launched (pipeline aabb + polygon, YOLO-only). **WTF-P v0.5.0 installed** locally into repo `.claude/` (145 files; STATE's "installed" claim had been false). `papers/shared/venues/` created; all 5 venue guides retrieved (3 Elsevier PDFs via user browser — ScienceDirect 403s bots) and distilled into per-venue `NOTES.md`. P1 new-paper interview started (paused at venue/argument questions). Work-log protocol adopted; this log backfilled. |
| Key issues | Case-insensitive Windows FS: `src/argusvision` ≡ `src/ArgusVision` → legacy renamed `src/_legacy_ArgusVision`. Evaluation slower than legacy per image (exact polygon IoU + 3 thresholds) — vectorization headroom noted, evaluator stays frozen. |
| Findings | **F7** legacy GSD loading was a silent no-op (hardcoded `dataset/dota_gsd_mapping.json`; file lives under `DOTA_v1/`). **F8** unanchored `.gitignore` patterns excluded `src/datasets/` from git entirely (absent even from thesis tag; rescued) and had kept SAM-eval `metrics.json` files untracked. **F9** legacy mask-ordering latent bug under mixed prompt configs (never fired; now structurally impossible, test-pinned). **Venues:** ISPRS OJPRS has NO data-paper article type → P1 frames as application Paper; DiB template mandatory; EarthVision deadlines are ~Mar (not ~Nov as roadmap assumed) → IGARSS 2027 (~Jan) comes first for P3. **Validation run 1 (aabb mode):** legacy V2 pipeline numbers reproduced to ±0.0005 (TP/FP/FN within 5 of 28k) — port faithful; first-ever honest macro-F1 and mean-AP recorded in `results/experimental/restructure_validation_pipeline_aabb/`. |
| Notes | **All three validation runs passed:** (1) port faithful ±0.05 pp vs legacy V2; (2) AABB→polygon ≤0.3 pp; (3) greedy→Hungarian delta ZERO at dataset scale — thesis YOLO macro-F1 0.6240 reproduced exactly. Report: `documentation/RESTRUCTURE_VALIDATION.md`. **`AV_dev` merged to `dev` (`59f583d`)**; one STATE-log conflict resolved (kept both rows); 62/62 tests on merged tree. Process slip owned: a `git add -A` swept the dev-bound pile into the Phase-4 commit on `AV_dev` — absorbed harmlessly by the immediate merge. Roadmap → v2.2 (P1 → Data in Brief; P3 deadline correction). P1: `.planning/` + 5 section drafts + `master.bib` seeded; DiB template v19 rules extracted from official .docx. Deletion inventory in `TODO_RESTRUCTURE.md`; `evaluate_sam.py` deliberately NOT superseded — port as driver kind at P4 prep. **Legacy branch preservation:** created and pushed `legacy_thesis` at `258a67c`, preserving thesis-era structure plus rescued `src/datasets/` dataloader scripts only; no dataset payloads added. **Branch hygiene:** deleted merged `AV_dev` locally and from `origin`. |
| Files changed | `ArgusVision_Strategic_Planning/PUBLICATION_ROADMAP.md`; `Athena_Protocols/{ATHENA_STATE.md,WORK_LOG.md}`; `TODO_RESTRUCTURE.md`; `pyproject.toml`; `.gitignore`; `ArgusVision_environment.{yml,lock.txt}` (+pytest); `src/argusvision/**`; `src/datasets/**`; `experiments/**`; `tests/**`; `.claude/**`; `papers/shared/venues/**` |
| Verification | 62/62 pytest in `AV_env`; validation run 1 diffed against `results/experimental/ArgusVision_v2_evaluation/evaluation_metrics_v2.json`; manifests carry clean git SHA. `legacy_thesis` tracks `origin/legacy_thesis`; `git check-ignore` confirms `src/datasets/aerial_fuse_cv_dataloader.py` is trackable while `dataset/DOTA_v1/...` remains ignored. `AV_dev` no longer appears locally or under `origin`. |
| Next actions | User-gated: legacy `src/` deletion sweep. P0 Zenodo (Aug) — unblocks P1 Data Description + methods. Verify DiB APC. P2 prep: MMDetection-on-Windows (STATE OPEN 5), driver class_map passthrough for any VBB eval. ~~Resolve `LEGACY_BRANCH.md` worktree state~~ — done 2026-07-30, see below. |

### 2026-07-30 — Mode(s): OPERATOR

| Field | Entry |
|---|---|
| Work hours | ~1.5 h (00:15 → 01:30 EEST) — post-midnight continuation of the 2026-07-29 session |
| Work done | **P1 writing (WTF-P, sections sourced from thesis §3.1 + code):** Methods drafted against the thesis's five published stages with implementation line-referenced to `refine_aerialfusecv.py`; Data Description drafted from Tables 6–7 (organisation, formats, GSD-inherited image properties, construction funnel, per-category stats, category distribution); Limitations → v0.3; abstract corrected twice. P1 now has drafts of every section except ethics/CRediT boilerplate. Earlier: resolved the `LEGACY_BRANCH.md` misplacement flagged at the previous close: the orientation note had been committed to `dev` (`293e89a`) but describes the frozen snapshot, so the active branch carried a file reading "do not develop on it". Removed from `dev`, rewritten and committed on `legacy_thesis` (`1093ff2`) with the frozen-state warning, tag-vs-`ArgusVision_main` rationale, F8 restoration record, and the list of deliberately preserved defects (F1/F2/F7/F9). Recorded the full branch layout in `ATHENA_STATE.md` § Infrastructure, which had no mention of `legacy_thesis` at all. |
| Key issues | **Wrote paper prose from recollection instead of source.** The first Methods draft described instance isolation as "clipping with the DOTA oriented-box polygons" — that is the *evaluation* code's method, not the dataset's. Caught only by reading the thesis and script. Rule reinforced: every P1 section is sourced from thesis §/table or code line before it is written. Ledger gap (earlier): the preservation branch existed on the remote but was invisible to any session reading only `ATHENA_STATE.md`. |
| Findings | **🔴 P0 blocker (STATE OPEN 6): construction Stages 1 and 3 exist in no committed script.** Only Stage 2 (`convert_dota_to_yolo_obb.py`) and Stage 4 (`refine_aerialfusecv.py`) survive as code; the DOTA∩iSAID intersection and mask reorganisation are absent, and there are no notebooks. P0's deliverable is a rebuild script, so both must be authored before release — thesis §3.1 specifies them well enough to reimplement. **"97.9% match rate" is a box-side rate only:** summing Table 7's mask columns gives ~330,693 iSAID mask instances in the 1,869-image subset, only ~37.8% paired; ~205,591 mask instances (mostly small vehicles — iSAID marks 6.2× more than DOTA boxes in train) are absent from released masks. Derived, not stated in the thesis → flagged ⚠ for regeneration from `Dataset_Statistics.md` at P0. Now disclosed in Data Description, Limitations and the abstract. **Three thesis prose errata** recorded in STATE so papers do not inherit them (moderate group miscounted "seven"/six, rare group "five"/six, lowest-performer list omits soccer-ball-field 92.2%); the tables themselves are arithmetically sound. Earlier: `legacy_thesis` verified correct as built: based on tag `thesis-code-final` (`11ba53c`), carries the thesis-era flat `src/` layout plus the rescued `src/datasets/` dataloaders and the anchored `.gitignore`. Confirmed `ArgusVision_main` is NOT a valid legacy anchor — it is 6 commits older and its environment spec predates the corrections, so it could not rebuild the env that produced the thesis results. |
| Notes | No code touched; documentation, papers and ledger only. Thesis PDF extracted to scratchpad with pypdf (no poppler in WSL, so the Read tool cannot render PDFs — pypdf text extraction is the working method for this machine). |
| Files changed | `LEGACY_BRANCH.md` (removed from `dev`, added on `legacy_thesis`); `Athena_Protocols/{ATHENA_STATE.md,WORK_LOG.md}`; `papers/p1_aerialfusecv_descriptor/sections/{00_front_matter,04_data_description,05_methods,06_limitations}.md` (+ skeleton deleted); `papers/p1_aerialfusecv_descriptor/.planning/structure/outline.md` |
| Verification | All three branches in sync with `origin`, no ahead/behind. `LEGACY_BRANCH.md` present on `legacy_thesis` only (absent from `dev` and `ArgusVision_main`). 62/62 pytest on `dev` in `AV_env`. Worktree clean. |
| Next actions | **P0 now has a hard prerequisite: author + verify construction Stages 1 and 3 (STATE OPEN 6)** before the Zenodo release; regenerate the ⚠ mask-side statistics from `Dataset_Statistics.md` and replace the derivations in P1. Then: P1 ethics/CRediT boilerplate, 4 figures, 🔴 REVIEWER pass. Unchanged: user-gated legacy `src/` deletion sweep; verify DiB APC; MMDetection-on-Windows (OPEN 5). |

### 2026-07-31 — Mode(s): OPERATOR / REVIEWER / ADVISOR

| Field | Entry |
|---|---|
| Work hours | **~16.4 h** (07:46 → 00:07 +1d EEST; timed from first to last commit — earlier uncommitted work not counted) |
| Work done | **AerialFuseCV rebuilt correctly and verified.** Regression build (semantic mode) reproduced the precursor dataset **exactly** — 1,857 labels and 1,857 masks compared, 0 mismatches, pair delta +0 — proving port fidelity before anything else was trusted. Then four settled changes implemented in one rebuild: (1) matching scores the **rasterised oriented polygon** against the instance mask instead of the axis-aligned hull; (2) both `labels_obb/` and `labels_hbb/` written, HBB as a clockwise hull quadrilateral; (3) annotations kept **DOTA-native** (headers, absolute px, class names, `difficult`); (4) parallelised across images. Corrected build: **125,722 pairs** (98.34% box pairing), 1,862 images, matched-IoU median **0.715**, in 24 min (4.1× speedup). Self-consistency audit passed: file counts, label-line counts, `pairs.jsonl` count and reported pairs all agree; **every one of 125,722 HBBs verified to be the hull of its OBB**; stale-GSD defect (R3) fixed. Pipeline experiment run on the corrected dataset and compared against the polygon-mode baseline. Built `dataset/verify_build.py` and `dataset/inspect_gt_masks.py`. |
| Key issues | `pkill` from WSL does not kill the Windows-side Python launched via interop — file locks persisted until the real PIDs were terminated (identified by command line first). Git operations time out during large builds because git walks the tree even for ignored paths. `conda run` buffers stdout, so a background log stays empty until exit — "no output" is indistinguishable from "crashed" unless the script creates visible artefacts early. |
| Findings | **Hull vs polygon:** the axis-aligned hull is a median **1.83×** the true oriented box area (68% >1.5×, 37% >2×) over 21,265 boxes; DOTA is genuinely oriented (95.5% rotated, median 17.1° off-axis). Fixing this gained **+620 pairs** and **+0.247 median IoU**; biggest class gains harbor (+548, 90.6%→97.4%) and plane (+265) — both strongly oriented. Negative deltas in ship/small-vehicle/large-vehicle are the one-to-one constraint removing duplicate claims, i.e. a correctness gain. **Experiment on the corrected dataset:** detection improved at every threshold (precision +0.0057, mean AP +0.0131) — the false-positive artefact shrinking as GT becomes more complete; segmentation flat, **because the evaluation never reads the new instance masks**. **Licensing:** iSAID carries DOTA's exact restriction (annotations academic-only, commercial prohibited) plus Google Earth terms; `pairs.jsonl` as built still reproduces source annotation content, so the deposit must **reference** rather than reproduce. **The big one — annotation discrepancy:** polygon-clipped GT and exact iSAID instance GT agree on only **18.5%** of instances (mean IoU 0.868). Visual inspection overturned the "contamination" explanation: the worst cases show **`extra=0px`** — DOTA and iSAID annotate **different object extents** (baseball diamond = infield vs whole field; harbor = pier vs pier+platforms), plus outright iSAID errors (P0262: one instance covering a boat, its dock, and a second boat — verified 1 colour, 1 connected component, so not an artefact of our extraction). Separately, **2.4% of colour-instances span multiple disconnected pieces** — the only part that is ours to fix. |
| Notes | The visual-inspection request prevented a bad change: I had recommended switching GT to iSAID instance masks; the figures showed that would score box-prompted predictions against extents the prompt never covered. Recommendation withdrawn. New direction agreed: quantify the discrepancies, argue them in the paper, and release **reconciled masks** derived from box-clipping — with the caveat that "100% agreement" is circular unless framed as evaluation validity, and that clipping does not remove neighbour contamination (unmeasured). Venue deferred; may outgrow Data in Brief. |
| Files changed | `dataset/{build_aerialfusecv,verify_build,inspect_gt_masks,scan_isaid_colours}.py`; `src/argusvision/data/aerialfusecv.py`; `src/argusvision/evaluation/evaluator.py`; `experiments/configs/p2_pipeline_aerialfusecv_v1.yaml`; `TODO_RECREATION_SCRIPT.md`; `documentation/DECISION_unpaired_annotations.md`; `Athena_Protocols/ATHENA_STATE.md`; `.gitignore` |
| Verification | Regression build vs reference: 3-level comparison, PASS with zero mismatches. Corrected build: self-consistency audit passed on all checks. 62/62 pytest. Colour audit re-run reproduced its own earlier numbers exactly (1,869 images, 16 colours, 0 unknown, 0 multi-class instances, 475,438 instances). |
| Session close | **Steps 1–6 of `TODO_AERIALFUSECV_REBUILD.md` executed autonomously.** Discrepancy measured over all 125,722 pairs: 90.0% extent / 10.0% foreign, 16.3% of pairs contaminated, 2.61% multi-piece instances. Reconciled masks defined (matched instance ∩ oriented box) and recorded in `documentation/DECISION_reconciled_masks.md`; `dataset/AerialFuseCV` built. **Second finding of the same shape as the first:** the re-run showed exactly zero delta because the evaluation still re-derived GT by clipping the semantic mask and never read the released instance masks — `AerialFuseCVSplit.ground_truth_masks()` now resolves each label line to its iSAID instance via `pairs.jsonl`. Re-run then gave +0.0012 seg IoU, small but with per-class gains falling **only** on the classes the discrepancy measurement independently flagged as contaminated (plane, storage-tank, small/large-vehicle) and exactly 0.000 elsewhere — two independent measurements agreeing. EDA + 4 figures generated. Consolidated evidence: `results/experimental/AerialFuseCV_Testing/FINDINGS.md`. |
| Next actions | **Steps 7–8 deliberately not executed.** Step 7 (deliverable script) is blocked on licensing answers — NTUA research-data office, DOTA/iSAID authors, venue pre-submission enquiry — and step 8's deletion is gated on step 7's verified build. Remaining: make the extraction component-aware (step 2 residual, 2.61%); re-source P1 numerically from `dataset_statistics.json`; decide venue once the discrepancy analysis is sized; `argusvision` VBB→HBB rename. |

## 2026-08

### 2026-08-03 — Mode(s): STRATEGIST / OPERATOR / REVIEWER

| Field | Entry |
|---|---|
| Work hours | in progress (started before 12:00 EEST) |
| Work done | Added `ArgusVision_Strategic_Planning/Context/` as ATHENA's strategic context inbox; wired the survey rule into `ATHENA_RISE.md` and identical bootstrap stubs; created `Context/README.md`; reviewed `2504.09203v1.pdf` as `2504.09203v1_AerOSeg_REVIEW.md`; updated `ATHENA_STATE.md`. |
| Key issues | Strategic planning previously had no durable intake path for newly found papers/competitor context, so future sessions could miss evidence unless it had been promoted to the roadmap or state ledger. |
| Findings | AerOSeg uses SAM as frozen feature guidance for trained open-vocabulary semantic segmentation, not as an OBB/prompted instance segmenter. It does not kill the OBB-for-SAM prompt idea; it does strengthen related-work expectations around SAM use in remote sensing. |
| Notes | No roadmap revision required now. AerOSeg is a related-work anchor for SAM+CLIP remote-sensing segmentation and possible P4 context baseline family only if P4 expands beyond instance-level detect-to-segment systems. |
| Files changed | `ArgusVision_Strategic_Planning/Context/{README.md,2504.09203v1_AerOSeg_REVIEW.md}`; `Athena_Protocols/{ATHENA_RISE.md,ATHENA_STATE.md,WORK_LOG.md}`; `AGENTS.md`; `CLAUDE.md`; `GEMINI.md` |
| Verification | `git diff --check` passed; `AGENTS.md`, `CLAUDE.md`, and `GEMINI.md` confirmed byte-identical. |
| Next actions | Keep surveying `Context/` on startup and before strategic advice; if more papers are added, create review notes before letting them affect roadmap decisions. |

### 2026-08-04 — Mode(s): ADVISOR / STRATEGIST / OPERATOR

| Field | Entry |
|---|---|
| Work hours | **5.3 h** (09:29 → 14:48 EEST, measured from the first to the last commit of the day; session still open at time of writing) |
| Execution mode | Parallel until ~14:10, then **single-operator** (user directive). F6b was already running when the mode changed and was allowed to finish. |
| Work done | (1) iSAID positioning + competitive review → roadmap v2.3. (2) Fast track → roadmap v2.4. (3) Need-statement locked. (4) Multi-model orchestration plan drafted. (5) **Corrected coordination:** created `ACTIVE_TASKS.md`; claim discipline in RISE v2.1.1 + bootstrap stubs; rejected vendor model matrix as authority; F1→F4 serial; F1 claimed here single-threaded; reviewer-only parallel OK. (6) **F6 worker task:** cloned `AV_env` to separate `P2_env`; probed Mask R-CNN options; created AerialFuseCV→COCO converter; created torchvision Mask R-CNN smoke script; ran 50-image COCO conversion and 50-iteration CUDA smoke training. (7) **F6b worker task:** built a tiling evaluator wrapper, measured full-image vs 1024px tiled YOLOv11x-OBB on the 456-image AerialFuseCV val split, and wrote `experiments/tiling/REPORT.md` plus metrics/manifest under `results/experimental/f6b_tiling_yolo11x_obb/`. (8) **Merged F6 and F6b** into `dev` after audit (branch hygiene, no >1 MB artefacts, controlled comparison, provenance). (9) **Mode switch:** RISE → v2.2 — execution mode is now asked at every session start and never inherited; parallel machinery retained dormant. (10) Returned the shared checkout to `dev` and removed the stale `.worktrees/f1`. (11) Restarted the F1 deposit rebuild (correspondence regenerated: 125,722 pairs, 0 unresolved). (12) **Structural review:** platform-vs-experiment boundary defined; tiling identified as platform capability sitting outside the package. |
| Key issues | Model-brand allocation is thinner evidence than lock discipline. Parallel P1 writing during F1 mostly wastes rewrite cycles. Control files are the real collision surface. For F6, MMDetection stack (`mmdet`, `mmcv`, `mmengine`) was absent from `P2_env`; fallback path was selected to avoid destabilizing `AV_env`. For F6b, ignored datasets/checkpoints are not copied into Git worktrees, so the runner executes from `.worktrees/f6b` but resolves assets from `D:/Work/AV`. Windows `conda run` buffered progress until exit; run manifests needed a Windows worktree Git-path fallback. |
| Findings | Context continuity > model choice. Orchestrator = repo-connected session. Genuine parallelism now = licensing admin + hostile review notes. Code parallelism at F6–F8. F6 found a working Windows Mask R-CNN route through `torchvision.models.detection.maskrcnn` in `P2_env`, with CUDA available and a completed 50-iteration smoke run showing lower last-window average loss than first-window average loss. F6b found that tiling materially improves YOLOv11x-OBB detector quality under the frozen evaluator: at IoU 0.5, micro F1 0.7087→0.7918, macro F1 0.6022→0.6801, mean AP 0.4869→0.6687, with the biggest recall gains on small-vehicle, ship, and plane. Precision drops because final detections increase after tiling/NMS, so F8 should tune confidence/NMS via PR curves. |
| Notes | **Correction:** F1 is owned by the Claude orchestrator session, not Cursor — the earlier line in this entry was wrong. Do not hand F1 off for fake parallelism. F6 timing/loss values are operational smoke evidence only; no paper claims come from this task. `AV_env` was not modified. F6b used the same frozen `DetectionEvaluator` for both full-image and tiled detections; no `src/argusvision/**`, `dataset/**`, `papers/**`, or `results/experimental/AerialFuseCV_Testing/**` files were edited. |
| Files changed | `Athena_Protocols/{ACTIVE_TASKS.md,ATHENA_RISE.md,MULTI_MODEL_ORCHESTRATION.md,ATHENA_STATE.md,WORK_LOG.md}`; `CLAUDE.md`; `GEMINI.md`; `AGENTS.md`; roadmap; Context notes; `dataset/convert_aerialfusecv_to_coco.py`; `experiments/maskrcnn_setup/**`; `experiments/tiling/**`; `results/experimental/f6b_tiling_yolo11x_obb/**` |
| Verification | ACTIVE_TASKS has F1 claim; RISE startup lists ACTIVE_TASKS; orchestration plan parallelism section corrected. F6: `P2_env` probe recorded; COCO smoke JSON created; torchvision Mask R-CNN completed 50 CUDA iterations; F6 scripts passed `py_compile`; `git diff --check` passed for touched F6 files. F6b: two-image smoke run passed; full 456-image run completed in `AV_env`; F6b script passed `py_compile`; `git diff --check` passed; metrics and manifest present. |
| Next actions | Single-operator sequence, one task at a time: (1) **F1** — finish the deposit rebuild, verify against the internal build, write the verification artefact to `results/experimental/AerialFuseCV_Testing/deposit_verification/` so the reproducibility claim has a `results/` source. (2) **Platform hygiene before F7** — move tiling + rotated NMS into `argusvision/runtime/tiling.py`, add a branded `argusvision` console entry point with a `status.json` heartbeat so experiments run in the user's own terminal and are polled on request, relocate `experiments/maskrcnn_setup/` to `results/`. (3) **F2** Zenodo package → user uploads → DOI. (4) **F4** patch DOI into the P1 draft. (5) **F7** baselines, tiled, via a new `InstanceSegmenter` protocol and `models/baselines/`. Open defect carried to F8: the tiling evaluator has no tile-border truncation handling, so objects spanning a seam are double-counted; quantify before P2 numbers are final. User-owned: DiB reply, Zenodo upload, authorship/CRediT. |

### 2026-08-04 (session 2, resumed after accidental close) — Mode(s): OPERATOR / REVIEWER — **CLOSED 2026-08-05 10:31 EEST**

> **Rolling entry, updated every ~30 min of work so the sequence is restorable
> if the session closes.** **CLOSED 2026-08-05 10:31 EEST** — the deposit rebuild it was held open for completed and its outcome is recorded (7,448/7,448 checksums PASS, commit `6c7116f`). The session closed accidentally ~17:55 and was resumed 18:04; the rebuild survived as a detached process and was never interrupted. Work continues in the 2026-08-05 entry below.

| Field | Entry |
|---|---|
| Execution mode | **Single-operator** (confirmed at resume). |
| Work done | (1) Bootstrap re-run after accidental close; state re-established from commits rather than memory. (2) **Finished the config reorganisation** left staged: `configs/{baselines,argusvision,validation}/`, filenames stating the whole pipeline, `experiments/README.md` index, experiment identifiers realigned so `results/experimental/<experiment>/` mirrors the config tree, 3 superseded configs deleted (`a6a314b`). (3) **Found two colliding deposit-rebuild processes** writing one output tree (`--image-mode none` and `--image-mode copy --verify`); killed both, deleted the corrupted mixture, restarted a single clean run. (4) **Measured the tile-seam defect** over the full val split and committed it (`ce3b66d`). (5) Wrote `experiments/tiling/sweep_operating_point.py` — queued, not run. (6) **Created `Athena_Protocols/LESSONS.md`** — 15 operational lessons distilled from 2026-07-28 onward, each as what happened → what it cost → the rule now; 3 marked `⚠ REPEATED`, whose own rule is that a recurrence means the *rule* is wrong. Wired into the RISE startup sequence (step 3, before acting) and the three identical bootstrap stubs, with a close-of-session promotion rule. (7) Rolling/idle work-log rule added to RISE + stubs + this file's protocol; 20-min heartbeat monitor armed so an idle session still logs. (8) Surveyed P1: `DRAFT_P1.md` is now **v1.0, re-sourced from `results/`, zero ⚠ markers**. (9) **Hostile REVIEWER pass on P1** (`0cf5e00`): six findings fixed, one material — the abstract claimed the deposit ships DOTA annotations and iSAID masks, which both licences forbid and which the deposit does not do (it publishes `correspondence.jsonl` with a *box index* into the user's own file). Near miss recorded as LESSONS C5: I wrote '15 images report exactly 0.000 m/px' from nothing; the artefact says zero do, and 21 carry 1.34e-06. (10) Protocol: **a log entry may not be closed while a background job runs** (user directive) — added to WORK_LOG protocol, RISE, the three stubs and LESSONS D5. (11) **Second hostile review addressed** (external): confirmed B1 (deposit described four contradictory ways — abstract fixed earlier, four other sections still wrong), B5 (mandatory AI declaration absent), S5 (guide caps the abstract at 250 words, not the template's 500 — ours was 319), S6. Fixed B1/B2 across Specifications, Data accessibility, Organisation (deposit tree now shown separately from the rebuild product) and Limitations; added the AI declaration; abstract cut to exactly 250; softened S1 'foreign pixels cannot arise at all' to match the 2.61% colour-reuse residual; S3 reframed as evaluation validity; S4, M3, M4, M5 corrected. **M1 exposed a worse problem than reported:** emitting figure values to `results/` revealed the hand-picked P0262 ship case had silently stopped being the case described — rematching in the reconciled build paired that ship with a different, smaller instance (22 px excess, not the boat+dock+boat story). Figure 4 case selection is now driven by measured excess so it cannot drift. (12) **All four P1 figures generated and wired in** (`8253296`): F1 paired-example (P0259, 8 ships, scene chosen by rule not by eye), F2 class distribution (existed, verified), F3 construction pipeline (every number read from `dataset_statistics.json` so the schematic cannot drift from the build; shows both discard streams including the 349,716 unpaired iSAID instances), F4 source divergence (baseball-diamond convention difference + P0262 iSAID error — harbor dropped, its ~194 px excess understated the effect). Generators: `dataset/make_p1_figures.py`, `dataset/make_pipeline_figure.py`. Placeholders 15 → 12. (13) **Second accidental session close (~17:55).** Recovery confirmed the D3 rule works: the rebuild was a detached `cmd.exe` process, unaffected by the close, and progress was recoverable from the output tree alone — but its stdout was redirected into the dead session's job directory, so **the log was lost and progress had to be reconstructed by counting files**. New lesson candidate: a long job's log must be written to a repo-relative path, never a session-scoped one. (14) **B6 addressed** (second hostile review, data-ownership): added `### Provenance and ownership` to Data Description (309 words) making the argument DiB requires — the deposit is not a copy, subset or reformatting of either source; each `correspondence.jsonl` record is an assertion neither source makes and that cannot be read out of them; `discarded.jsonl` records the 2,121 unmatched boxes and 349,716 unmatched instances (both derived from `20260804_095041_d4c191a/dataset_statistics.json`, not from memory — C5 discipline); all six deposited files produced by the author at NTUA; sources used as inputs under academic terms and never redistributed. Ethics Statement extended to state ownership and to say annotations as well as imagery are not redistributed. (15) **F1 closed on evidence.** Wrote `dataset/verify_deposit_rebuild.py` — a standalone checker, separate from `rebuild --verify`, because an inline check that prints to a lost stdout proves nothing. Result: **7,448/7,448 PASS**, 0 missing, 0 mismatched, in 38 s. Artefact committed to `results/`. |
| Key issues | **I/O contention is the binding constraint on this machine, not GPU.** Launching the seam measurement while the disk-bound deposit rebuild ran stalled the rebuild completely for ~5 min. My fault; lesson recorded. **Rate estimates for the deposit repeatedly wrong** (30 min → 6.5 h → 4 h → ~2.5 h) because DOTA images span 800 px to ~13,000 px, so per-file copy time varies by an order of magnitude — short windows do not extrapolate. |
| Findings | **Seam defect quantified — real but not the story.** Seam-adjacent detections are 7.1% of all and 2.4x more likely to be false (FP rate 0.5638 vs 0.2370). Removing the whole excess (748 detections, 8.9% of all FP) moves precision 0.7399 → 0.7574: **1.75 of the 11.3-point tiled precision drop, ~15%**. The other 9.5 points are genuine marginal detections. My earlier "tiled precision is contaminated" framing was too strong. A 6-image smoke run overstated the effect 2x (18.8% vs 8.9%) — small samples mislead here. Consequence: **the confidence/NMS operating point is the lever, not seam handling.** |
| Notes | Cleanup of ~6.9 GB of thesis-era example PNGs surveyed and **deferred by user** until reruns produce equivalents. Deposit `_deposit_test` to be deleted once its 7,448 checksums verify. |
| Files changed | `experiments/configs/**` (regrouped); `experiments/README.md`; `experiments/tiling/{measure_seam_defect,sweep_operating_point}.py`; `results/experimental/AerialFuseCV_Testing/seam_defect/` |
| Verification | 100 tests pass; every config's dataset path resolves. Seam report at `results/experimental/AerialFuseCV_Testing/seam_defect/seam_defect_report.json`. |
| **TODO SEQUENCE (restore point)** | **1. F1 deposit rebuild — COMPLETE AND VERIFIED (2026-08-05 10:31).** All 1,862 images written (train ~20:10, val ~21:25). Checksums re-verified independently: **7,448/7,448 PASS, 0 missing, 0 mismatched**, artefact at `results/experimental/AerialFuseCV_Testing/deposit_verification/deposit_verification.json`. See lessons E1/E2 below for the 13 hours lost between finishing and knowing it had finished → write artefact to `results/experimental/AerialFuseCV_Testing/deposit_verification/` → **closes F1** → delete `_deposit_test`. **1b. NEW BLOCKER — optimise the deliverable rebuild script before F2 packages it.** `deposit/rebuild_aerialfusecv.py` scans the whole instance mask per pair (`np.all(ins == colour, axis=-1)`) and allocates a full-image polygon buffer per pair, so cost is pairs x image pixels. On P1387 (1,718 pairs — second densest train image) that is ~34e9 element comparisons and it sat >11 min on one image with no output. A stranger running the deposit script will conclude it has hung. Fix: pack the mask once per image to an integer key and select instances against it; rasterise the polygon inside the box's bounding rectangle, not the full image. Then re-verify that output is byte-identical. **1c. P1 blockers from the second hostile review — need work, not edits.** **B3 box_index integrity:** the deposit resolves boxes by ordinal index into the user's own DOTA file, with no pinned source version, date or checksum, and no reported re-resolve rate. A user with a different DOTA download silently gets wrong objects. Fix: pin and publish a source checksum, and have `rebuild` report how many indices resolved to a class-matching box. **B4 no pair-correctness validation:** 125,722 pairs, no audit that a matched box and mask are the same physical object — IoU>=0.1 does not prove identity. Fix: manual audit of ~200 pairs stratified by IoU, reported as an artefact. This is the strongest evidence still addable. **B6 data-ownership:** DiB requires the repository store data 'produced and owned by the author or their institution'; ours is derived correspondence. The argument is never made in prose — desk-reject risk. **S7 licensing unconfirmed:** the academic-use derivation claim rests on unanswered emails (NTUA research-data office, DOTA/iSAID authors, DiB pre-submission enquiry). **2.** Run `experiments/tiling/sweep_operating_point.py` (queued behind the deposit for I/O reasons; one GPU pass, ~13 min) → choose confidence + seam-drop operating point. **3.** Decide protocol: tiled vs **fused** (full-image + tiled merged) — large objects exceed a 1024 px tile and are fragmented, small objects are lost at full scale; F6b's class-level evidence points at fusion. **4.** Record the methodology as a decision document before any rerun. **5.** Rerun sweep: 10 YOLO OBB + 25 YOLO VBB + 6 SAM configs + ArgusVision cascade (~43 runs, ~17 GPU-h if both protocols). **6.** Then the deferred ~6.9 GB cleanup. |
| Next actions | **Deposit script defect found (see TODO 1b) — blocks F2, not tonight's run.** Deposit rebuild is through the image phase (1,862/1,862) and into `--verify`; the density spike that dominated 17:00–18:00 (P1387/P1388, ~1,500–1,700 pairs, ~13 min/image) cleared, and the back half ran at roughly 3.6 images/min. Every ETA given for this job was wrong (30 min → 6.5 h → 4 h → 2.5 h); the throughput driver is pair density, not file size or image count — a third recurrence of C1, and the reason no further ETA was offered. Nothing else may touch `dataset/`. **P1 while waiting:** placeholders are 4 deposit-dependent (DOI, URL, inventory), 6 user-owned (authorship, affiliation, institutional email, CRediT, acknowledgements, funding), 4 figures, 1 template dropdown. Figures done. **Remaining 12 placeholders are all non-prose:** 4 deposit-dependent (DOI ×2, direct URL, file inventory), 6 user-owned (authorship, affiliation, institutional email, CRediT, acknowledgements, funding), 1 template dropdown. Next offered: hostile REVIEWER pass on the prose — rated high because the draft was written by a session against numbers that have since changed twice (corrected build, then reconciled masks). |

### 2026-08-05 — Mode(s): OPERATOR / REVIEWER — **CLOSED 23:55 EEST**

> Closed with **nothing running** — Windows python processes 0, WSL job
> processes 0, pipeline status file corrected to reflect that the scripted run
> aborted at step 3 and steps 3-5 were completed manually after fixes.

| Field | Entry |
|---|---|
| Work hours | **~13.5 h** span (10:23 → 23:55 EEST, first to last commit). Much of it unattended job time — the discrepancy measurement alone ran 93 minutes and the first divergence scan 46. Active hours not tracked separately. |
| Execution mode | **Single-operator** (confirmed at resume, 2026-08-05). |
| Work done | (1) **Recovered from the second accidental close.** The deposit rebuild had in fact finished the previous evening; 13 h were spent believing it was still verifying, because the monitor was armed on `kill -0` of a WSL stub that never dies (see Findings). (2) **F1 closed on evidence** — wrote `dataset/verify_deposit_rebuild.py`, a standalone checker, and recorded **7,448/7,448 PASS** (`6c7116f`). (3) **B6 addressed** (`fc2dcaf`): `### Provenance and ownership` added to Data Description, plus an extended Ethics Statement — the DiB data-ownership argument that was never made in prose. (4) **Deposit script v2** (`03785e4`, `096c93d`): interactive prompts, real layout + cross-reference checks before any work starts, pair-driven progress bar, resolve-rate reporting, the 1b performance fix, and the seven root files the article documents but the old script never wrote. Output now always resolves into its own `AerialFuseCV/` folder. (5) **User ran it end to end on Windows against the real sources: 1,862 images, 125,722/125,722 pairs, resolve rate 100.00%, 0 problems, 606.7 s — and 7,448/7,448 checksums PASS.** A ~50x speedup that changed no byte of output. (6) `DATASET_ANALYSIS.md` written by the deliverable renamed to `summary.md`, resolving a filename collision with the internal EDA report of the same name; draft tree updated. (7) **Cleanup: 14.1 GB freed** — `dataset/_deposit_test` (14 GB) and `dataset/AerialFuseCV_regression` (118 MB, a superseded `--mask-source semantic` build). Two docstrings repointed off paths that no longer exist. (8) `dataset/make_eda_report.py` written as an **internal** single-file EDA — explicitly not produced by the deliverable; a user gets `summary.md` and the published article. (9) **Fixed `measure_discrepancy.py` before it could corrupt the paper** (see Findings) and launched the full re-measurement. (10) `dataset/regenerate_p1_artefacts.sh` — serial pipeline regenerating every P1 artefact from the new build. |
| Key issues | The divergence numbers in the draft (Tables 5–6, agreement 0.918/0.946) were measured on `dataset\AerialFuseCV_v1`, **a build that no longer exists**, while every other number comes from `d4c191a`. They have had no live source behind them for days. This is the same drift that silently invalidated the Figure 4 P0262 case. |
| Findings | **E2 — `kill -0` cannot detect a Windows process exiting.** The rebuild ran as `python.exe` on the Windows side; PIDs 71033/71055 were only the WSL `bash` and `/init` relay stubs, and they stay in state `S` forever after the real process exits. The monitor armed on them could never have fired. My completion criterion was wrong too: I waited on top-level artefacts the rebuild script never writes. Cost 13 h. Liveness across that boundary must be checked with `tasklist`, or better, by a terminal marker file. **E1 — a long job's log must live in the repo.** The rebuild's stdout went to the dead session's job directory, so its verification result — the one thing the multi-hour run existed to produce — was lost and had to be recomputed. **Discrepancy measurement was measuring the wrong thing.** `measure_discrepancy.py` read the "exact" instance from the dataset's own `instance_masks/`. That worked when the build stored unclipped instances; the reconciled build stores them **clipped** (verified: new `mask_px` 3733 == old `clip_px` 3733, against old `exact_px` 4042). Run unchanged it would have compared the clip with itself and reported near-perfect agreement over 93 minutes — a confidently wrong result that happens to support the claim we want to make. Now reads both masks from the original iSAID; only the boxes come from the build. Smoke run over 6 images: agreement 0.927/0.943, consistent with the old 0.918/0.946, so the comparison is real again. |
| Notes | **Box/mask disagreement reframed (user directive).** The released mask *is* the instance clipped to the box, so out-of-box pixels are not in the product; summing them describes the sources, not the dataset, and a 17M-pixel total reads as though iSAID were broken. Reporting moves to a **per-object** count at a stated ratio of **outside ÷ inside**, flagged above **1/3**. The whole threshold curve is published, as Table 3 already does for IoU acceptance, so the cut point is one row of a reported distribution rather than a tuned parameter. This changes what is *reported*, not what is *shipped* — no rebuild, no new checksums. Pair **identity** (B4) remains a separate open question: a mask can sit wholly inside its box and still be the wrong object. |
| Files changed | `papers/p1_aerialfusecv_descriptor/deposit/rebuild_aerialfusecv.py`; `papers/p1_aerialfusecv_descriptor/DRAFT_P1.md`; `dataset/{verify_deposit_rebuild,make_eda_report,measure_discrepancy,verify_build}.py`; `dataset/regenerate_p1_artefacts.sh`; `Athena_Protocols/{WORK_LOG,LESSONS,ACTIVE_TASKS}.md` |
| Verification | Deposit rebuild verified twice independently, 7,448/7,448 PASS both times (`6c7116f`, and again on the user's fresh Windows build). Resolve rate 100.00%, 0 problems. All touched scripts pass `py_compile`; `regenerate_p1_artefacts.sh` passes `bash -n`. Discrepancy fix smoke-tested on 6 images before the full run. |
| **TODO SEQUENCE (restore point)** | **1. DONE — P1 regeneration pipeline complete.** All artefacts rebuilt from the verified build: discrepancy re-measured (`annotation_discrepancy/`), EDA + 10 figures (`eda/`), three divergence candidates `F4a/F4b/F4c`, consolidated `AerialFuseCV_EDA.html` (50 MB, internal). **2. NEXT — rewrite draft Tables 5 and 6** with the re-measured numbers; the current ones came from `AerialFuseCV_v1`, deleted, so they have no surviving source. Foreign share moves 10.0% → 13.6%, foreign-pair incidence 16.3% → 17.7%. **3.** Add the outside÷inside curve with the 1/3 row (user's criterion: outside ÷ **inside**, i.e. >25% of the whole mask), publishing every threshold as Table 3 already does for IoU acceptance. Compute from `annotation_discrepancy/per_pair.jsonl` — no new run needed. **4.** User picks one of `F4a/F4b/F4c`; rename to `F4_source_divergence.png` and rewrite the Figure 4 caption from `figure_values.json`. The stale single-figure `F4_source_divergence.png` is still on disk and is NOT from this run — delete it when the choice is made. **5.** Re-verify every other number in the draft against the new artefacts before any of it is considered final. **6.** Remaining hostile-review items: **B3** source checksum pinning, **B4** ~200-pair identity audit, **S7** licensing (user-owned, F3). **7.** `SETUP.md` download/extract guide. **8.** Then F2 Zenodo packaging. |
| Lessons promoted | **F1** a file one tool writes and another reads is a schema contract — copy field names from the existing producer, grep every consumer before the first run. **F2** arithmetic identities catch what crashes do not; assert them where quantities are emitted. **D6 recurrence** — the pairs x pixels defect was reproduced in new code hours after the lesson was written, because the rule said *shipped* and the scan is internal. Rule corrected to cover any loop over pairs/instances/detections regardless of audience. |
| Next actions | Pipeline complete; nothing is running. **The draft is now the blocker, not the machine.** Tables 5-6 and the Figure 4 caption cite a build that no longer exists, and the replacement numbers are measured and waiting. Three separate wrong numbers were stopped today only by crashes or by arithmetic checks, never by the code reporting a problem — so every remaining number in the draft is treated as unverified until read back against `results/`. |

### 2026-08-06 — Mode(s): OPERATOR / ADVISOR / REVIEWER — **CLOSED 2026-08-06**

> Closed with nothing running: 0 Windows python processes, 0 WSL jobs, clean
> working tree. Execution mode **single-operator**, confirmed at session start.

| Field | Entry |
|---|---|
| Execution mode | **Single-operator** (user directive, confirmed 2026-08-06). |
| Work hours | Not tracked separately; session ran from bootstrap to close on 2026-08-06. |
| Work done | (1) Bootstrap re-run; state loaded through 2026-08-05. (2) **Divergence section of `DRAFT_P1.md` rewritten against the re-measured build** — user directive: DOI stays empty. Reframed per the user's argument that the pixel *sum* misleads, since the released mask is the clip and out-of-box pixels are not in the product; the section now leads with a **per-object** count. New **Table 5** gives the full outside÷inside curve; **5,850 objects (4.65%) exceed one third, 95.35% do not**. Old Table 5 → Table 6 (decomposition, now 86.4% extent / 13.6% foreign, was 90.0/10.0); old Table 6 → Table 7 (per category, all 15 rows replaced). Abstract updated and re-counted to **249 words** against the 250 cap. (3) Wrote `outside_inside_curve_report.json` so Table 5 has a `results/` source rather than an ad-hoc computation. (4) **Five extra divergence panels** for hand-composition (`eda/figures/divergence_gallery/G1..G5`), one file each, ranked by absolute excess and gated at outside÷inside ≥ 0.20 so a large object with a trivial overhang cannot crowd out a small one that is plainly wrong; five distinct classes, all arithmetic-checked. (5) **Audited every remaining number in the draft against `results/`.** One error found and fixed — see Findings. (6) **DiB editorial ruling received and acted on.** The Executive Editor rules the correspondence-plus-script deposit *"completely within scope"* and *"fully compliant"*, and derived/fused datasets in scope — recorded as `Context/DiB_editor_scope_ruling_2026-08_NOTES.md`. **B6 retired** (the hostile review's strongest structural objection) and **S7 downgraded** from blocker to courtesy. Seven attached conditions were worked one at a time, each verified with the user before the next: **M-i** licensing + non-commercial statement in Specifications Table and Value of the Data, terms quoted verbatim; **M-iii** official download-page URLs into the draft, References, `references.bib` and the deposit script — **both URLs fetched and confirmed live**, wording matching the 2026-07-31 record exactly; **Z-iii** `SETUP.md` written; **Z-i** the download divergence stated in Methods rather than emailed, plus three new preflight guards; **M-ii** the alignment audit; **Z-ii** already met; **Z-iv** package staged. (7) **Bibliography consolidated.** `papers/shared/master.bib` held 5 entries while P1 held 7, with 3 shared keys differing — stale for a week, nothing reporting it. Reconciled, then imported 83 entries from Zotero → **90 total**. Added `papers/shared/bib_tool.py` (merge/audit/extract/list, no third-party deps), matching on DOI and normalised title rather than key alone. (8) **Alignment audit built, reviewed and scored** — see Findings. (9) **Zenodo package staged, not released** (user directive: release with submission, so the dataset is never public with no paper claiming it). Resolves the CC-BY 4.0 contradiction → **CC BY-NC 4.0**. (10) Figures 4 and 5, word discipline against DiB limits, and a supervisor-reviewable `.docx` export with figures placed inline and all 11 placeholders listed up front. |
| Key issues | The draft's divergence numbers had been sourced to `AerialFuseCV_v1`, deleted on 2026-08-05, so Tables 5–6 asserted values with no surviving artefact behind them. Closed. |
| Findings | Foreign contamination is materially higher on the corrected build than the draft claimed: **13.6% of disagreeing pixels vs 10.0%**, and **22,294 pairs (17.73%) carry a foreign pixel vs 20,489 (16.3%)**. Per-category extent shares moved enough to invalidate the old prose claim that extent accounted for "essentially all" disagreement in the sports fields — soccer-ball-field and ground-track-field are now 87.4%, not ~100%. The sentence was rewritten to name only the seven categories above 96%. Multi-piece incidence is unchanged at 3,283 pairs (2.61%). **Audit found one wrong claim in the GSD paragraph:** the draft said 21 images "carry an implausibly small value of 1.34e-06 m/px". The count of 21 is right, but they span **five distinct degenerate values** from 1.34e-06 to 2.15e-05, and only **twelve** sit at 1.34e-06. Corrected, and the plausible remainder is now stated as 1,840 images. Everything else in the draft — Tables 1-4, the funnel, per-split and per-category counts, discard reasons, threshold sensitivity, instance-area range — verified exact. |
| Notes | Every number in the rewritten section was verified back against `discrepancy_summary.json` and `per_pair.jsonl` by script — 40 checks including all 15 category rows, all passing — rather than by reading. This is the F2 discipline adopted after three wrong numbers slipped through on 2026-08-05. |
| Files changed | `papers/p1_aerialfusecv_descriptor/DRAFT_P1.md`; `results/experimental/AerialFuseCV_Testing/annotation_discrepancy/outside_inside_curve_report.json` |
| Verification | **Full numeric audit of `DRAFT_P1.md` complete — 78 checks, all passing** after one correction. Covers Tables 1–7, the construction funnel, per-split and per-category counts, unpaired accounting, discard reasons, threshold sensitivity, instance-area range, GSD, and multi-piece incidence. The discards satisfy the accounting identity exactly: 351,837 discards = (127,843 − 125,722) boxes + (475,438 − 125,722) instances. 40/40 divergence-section checks pass against the artefacts. Abstract 249 words (cap 250). No stale divergence value (`0.918`, `0.946`, `90.0%`, `16.3%`, `20,489`, `1,689,081`) remains anywhere in the draft. |
| **TODO SEQUENCE (restore point)** | **1. DONE** — divergence section rewritten and verified. **2.** User picks one of `F4a/F4b/F4c` (`results/experimental/AerialFuseCV_Testing/eda/figures/`); then rename to `F4_source_divergence.png`, rewrite the Figure 4 caption from `figure_values.json`, and **delete the stale `F4_source_divergence.png` still on disk, which is not from the current run**. **3.** Re-verify every *other* number in the draft against `results/` — the divergence section is now clean, the rest is unaudited. **4.** Remaining hostile-review items: **B3** source checksum pinning, **B4** ~200-pair identity audit, **B6** done, **S7** licensing (user-owned, F3). **5.** `SETUP.md` download/extract guide. **6.** F2 Zenodo packaging → DOI → patch the DOI placeholders. **7.** Stale control file: `ATHENA_STATE.md` still lists `dataset/AerialFuseCV_Refined/` (14 GB) and other deleted trees — asset table wrong by ~28 GB. |
| Lessons promoted | **G1** prose describing an artefact rots silently when the artefact is rebuilt — Figure 4's caption described a build deleted days earlier, and Limitations self-reported 182 words while holding 203. **G2** a guard tested only where its failure cannot occur is untested — the case-sensitivity check passed on Windows without ever running. **G3** (⚠ REPEATED, E2 from the other side) piping a cross-boundary command into `head` orphans the far side; a test kept writing 305 files while `rm` fought it. |
| Next actions | **Everything remaining on P1 is user-owned.** Authorship and order (is Prof. Ioannidis a co-author?), affiliation and institutional email, CRediT, acknowledgements, funding, and the template Subject dropdown. Then six `[[VERIFY pages/DOI]]` citation checks against publisher records. Supervisor review of `exports/AerialFuseCV_DiB_draft.docx` is the long pole. Figure 4 remains provisional — the user may compose their own from F4a/b/c and the five gallery panels. On submission day: upload `zenodo_release/AerialFuseCV_v1.0/`, patch the DOI into four placeholders, transfer content into the official DiB `.docx`. |
